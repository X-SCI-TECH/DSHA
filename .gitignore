# Gradle / Android 构建产物
.gradle/
build/
app/build/
captures/
*.apk
*.aab

# Python 缓存
__pycache__/
*.pyc

# 本机配置（不提交）
local.properties
.idea/
*.iml
.DS_Store

# 离线 rootfs：不提交，CI 现场生成（AGENTS.md 约定；304MB 也超 git 单文件硬限）
app/src/main/assets/offline-rootfs.bin
app/src/main/assets/offline-rootfs.version

# 发布 APK：走 GitHub Release 资产，不进 git
release/*.apk
release/*.sha256
release/_data_backup/

# 本地临时/中间产物
tmp_*/
tools/offline-rootfs-old-v8.bin
python-runtime.tgz
scr_backup.png
