# DSHA 1.2 本地升级报告

## 2026-08-31 alpha2 插件专项 / vc104

- 目标：`1.2.0-alpha.2`，`versionCode=104`，applicationId `com.dsh.client`。
- dsh 固定 tag/commit：`dsh-v0.1.2-alpha.2` / `0a53fb55bea101816fa226bb964ae2bed71c343b`。
- `OFFLINE_VERSION=5`；manifest 已重生成并用内置公钥自验证通过（`SIGN_OK`）。
- 内置插件资产已纳入 provisioning/注册链路：`@dsh-external/dsh-mobile-nav`、
  `dsh-device-shell-guide`、`dsh-task-notifier`、`dsh-status-overlay`、`dsh-web-mobile`。
  `dsh-web-mobile` 来源 commit 为 `ebbd18b0a85730f9d5430755abe9cd2e21b8522d`，包含
  `package.json`、`cordis.patch.yml`、`lib/index.js`、`lib/client.js`、`lib/compress.js` 和 client 资源。
  provisioning 同时维护实体目录、profile dependencies/bundles、profile/global node_modules 链接和 marker，且保留官方 `@deepseek-ai/dsh-base`、`@deepseek-ai/dsh-web-app` bundle。
- Termux 原生 `aarch64` 独立 stage 已生成插件 rootfs：
  `out/offline-rootfs-alpha2-plugins.tar.gz`，SHA-256 `52cbfd5f06d0323d1a42e44cc5d695cd8af92b1a5eed4aa888dfadbeda82723d`。
  stage 内 `dsh --version` 为 `0.1.2-alpha.2`，五个插件实体及入口均存在，profile 注册文件已生成。
- loader 启动验证仍未完全通过：首次测试暴露全局解析链路问题，已补全 global node_modules 链接；复测剩余环境阻塞为 HMR 默认 loader（`--expose-internals is required`，需 App 运行时已实现的单次 profile 自愈）以及 stage 缺少 Python/临时目录与 3080 残留占用。插件 stage 已同步为工作树 `offline-rootfs.tar.gz` 并打入 APK；由于上述 loader 闭环尚未完成，仍不宣称为完整生产 rootfs 已验收。
- Windows 本地 Java 17/Gradle 8.5 构建通过：`:app:assembleDebug`、`verifyArm64Apk`；`testDebugUnitTest` 当前无测试源（`NO-SOURCE`）。
- 最终 APK：`release/dsha-1.2.0-alpha.2-vc104.apk`，SHA-256 `613b1f3801646e40852661722d238577501fd970279ee0fe61b784b54f80da`；校验文件为同名 `.sha256`。
  `aapt dump badging` 确认 `com.dsh.client` / `versionName=1.2.0-alpha.2` / `versionCode=104`；APK 仅含 `arm64-v8a` 库，`assets/offline-rootfs.bin` 160,372,143 字节且 ZIP method `STORED`。
  `apksigner verify --verbose --print-certs`：v2=true、v3=true，证书 SHA-256 `e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`，与历史证书匹配。
- 已执行：Bash `-n`（provision/offline/pack/stale 脚本）、Python `py_compile`（selftest/manifest）、`git diff --check`、manifest 生成/签名、Java 编译、APK 资源/ABI/签名检查。未使用 adb，未安装 APK，未删除设备数据或备份。
- 未完成硬阻塞：无法在当前 Windows 环境完成“真实 alpha2 loader 全部 active + BrowserAuth 运行时”闭环；Termux stage 需要补齐 Python/临时目录并清理旧 3080 进程后重跑。真机覆盖升级、LAN/WebSocket、完整 rootfs 生产替换均保持未验证。

## Alpha2 适配记录（2026-08-31）

- 本轮目标：`1.2.0-alpha.2`，`versionCode=103`，applicationId `com.dsh.client`。
- 固定 upstream：tag `dsh-v0.1.2-alpha.2`，commit
  `0a53fb55bea101816fa226bb964ae2bed71c343b`；`apps/cli/package.json` 已核验
  `0.1.2-alpha.2`。
- upstream tag archive 只读下载到 `F:\DSHA\_upstream-dsh-1.2-alpha2.tar.gz`，审计副本位于
  `F:\DSHA\_upstream-dsh-1.2-alpha2-extract`；未向 GitHub 执行任何写操作。
- Android 版本、`Constants`、offline provision/build 脚本和 runtime manifest 已切换到 alpha2；
  `OFFLINE_VERSION` 与 `app/src/main/assets/offline-rootfs.version` 已升为 `4`。
- 现有 `offline-rootfs.tar.gz` 内容实际仍为 alpha1（metadata 与 dsh package 均为
  `0.1.2-alpha.1`），因此本轮拒绝使用它构建 alpha2 APK；没有生成 alpha2 假包，也没有覆盖既有 alpha1 APK。
- 当前环境没有可控的原生 Linux arm64 用户空间：设备只读预检为 Android `aarch64`，但无 `su`，
  `unshare --net --fork true` 返回 `Operation not permitted`。ADB 仅能看到 Android shell 身份；
  `/data` 可用空间充足，但这不能提供 root chroot、挂载和隔离网络所需能力。按门禁未执行
  QEMU/WSL/Docker 替代构建。
- alpha2 的 `build:official`、release pack、离线 consumer install、生产 closure、rootfs/chroot WebAuth
  和 alpha2 完整 APK 均未宣称完成，待本地原生 Linux arm64 主机提供后再执行。

### Alpha2 本地验证结果

- upstream compare API 已核验 alpha1 commit 到 alpha2 commit 为 `ahead_by=234`、`files=300`；
  alpha2 tag archive 中 `apps/cli/package.json` 版本为 `0.1.2-alpha.2`。
- compare 中可核对到的主要上游变化包括 session projection/cache 与顺序历史读取性能、LLM 重试、
  Web schedule/reminder 展示、插件分组与 Agent Preset、Web Search 恢复提示、Turn-time/token 统计、
  RemoteError 统一词汇及 vendor 发布依赖更新；`apps/cli/package.json` 与 `pnpm-lock.yaml` 内容 hash 均不同。
- compare commit message 还明确包含 `release(vendor)`（含 hmr 1.0.17 等）、`perf(session-projection)`、
  `feat(ui-chat)` usage/time pills、`fix(atomic-write)` Windows replacement retry，以及保留
  `SessionEvent.ignorable` 的变更；这些 upstream Web/运行时能力未在 DSHA 侧重写，待 alpha2 closure
  完成后做真实 Web 验收。
- `python tools/gen-runtime-manifest.py --check`：`MANIFEST_OK`（35 个文件）；manifest 已包含 alpha2
  runtimeId、tag、commit 和 `offlineVersion=4`，现有公钥自验证签名返回 `SIGN_OK`。
- `tools/pure-logic-test.sh`：359 项通过；`tools/phase2-pure-test.sh`：alpha2 常量、脚本门禁、HMR/PTY、备份/LAN/插件检查全部通过。
- `tools/auth-url-test.sh` 34 项通过；`tools/backup-latest-test.sh` 通过；`tools/restore-scope-test.sh`、
  restore/rootfs 失败注入、归档/提取安全测试、资产脚本检查和 Python 编译均通过；全部 `scripts/*.sh`、
  `tools/*.sh` 通过 `bash -n`。
- Java 17 `:app:compileDebugJavaWithJavac`、`lintDebug` 均成功；`testDebugUnitTest` 成功但仓库无 JVM
  单元测试源（`NO-SOURCE`）。`-Pslim :app:assembleDebug -x verifyArm64Apk` 成功，aapt 只读核验为
  `com.dsh.client` / `versionName=1.2.0-alpha.2` / `versionCode=103` / `arm64-v8a`；该 slim APK
  不含 rootfs，已删除，不是交付产物。
- 完整 APK 构建被刻意阻断：当前离线包内 runtime metadata 与 `@deepseek-ai/dsh/package.json` 均为
  alpha1，打包门禁要求 alpha2，避免生成不可启动的混合版本。release 目录保留的旧文件仍为
  `release/dsha-1.2.0-alpha.1-vc102.apk` 及其 `.sha256`；`release/dsha-1.2.0-alpha.2-vc103.apk`
  不存在（没有伪造或覆盖旧产物）。
- 未执行 APK 安装、覆盖安装、卸载、`pm clear` 或任何设备数据写操作；设备仅做只读 ABI/版本/空间检查。

### 2026-08-31 alpha2 继续复核

- 设备 `fmuoeujvonizjj4d` 只读复核仍为 Android 13、`aarch64`/`arm64-v8a`；`su` 不存在，
  shell 身份为 uid 2000，不能执行要求的 root `chroot`/mount/隔离网络构建。Windows WSL 为 stopped，
  且不作为 arm64 构建替代。Termux 只读探测到 Node `v26.4.0`、git 和 proot，但没有 pnpm，
  也没有安装任何构建工具或修改设备数据；现有 Node 版本不满足固定的 `v24.19.0` 要求。
- 既有 `offline-rootfs.tar.gz` 直接读取的 runtime metadata 为
  `dsh-v0.1.2-alpha.1` / commit `cd5ef8148158c3a752a658978873241fdf8e2bbc`，
  `@deepseek-ai/dsh` 版本为 `0.1.2-alpha.1`；因此 alpha2 完整打包预检继续拒绝，
  `release/dsha-1.2.0-alpha.2-vc103.apk` 不存在。
- 使用项目私有 Temurin 17.0.20.1 + Gradle 8.5 串行复跑
  `:app:compileDebugJavaWithJavac`、`lintDebug`、`testDebugUnitTest`，返回 `BUILD SUCCESSFUL`；
  单元测试仍为 `NO-SOURCE`。`pure-logic-test` 359 项、`phase2-pure-test` 38 项、`auth-url-test`
  34 项、备份/恢复/rootfs 失败注入、资产脚本检查和 shell/Python 语法检查均通过。
- 本次继续复核没有执行 APK 安装、覆盖安装、卸载、`pm clear`、手机备份删除或 GitHub 写操作；
  旧参考树仍只读，目标树 remote 仍为空。
- 用户已确认“仅在 alpha2 rootfs 完整验证成功后替换旧 alpha1 rootfs，并保留 alpha1 APK 回滚包”的处理方式；
  由于 native arm64 closure/rootfs 尚未成功，替换前置条件未满足，本次没有删除、覆盖或生成任何 rootfs 备份副本。

## Alpha1 基线记录（历史）

- 目标版本：`1.2.0-alpha.1`，`versionCode=102`。
- 固定 deepseek-harness：tag `dsh-v0.1.2-alpha.1`，commit
  `cd5ef8148158c3a752a658978873241fdf8e2bbc`。
- 离线包版本标记：`OFFLINE_VERSION=3`，`app/src/main/assets/offline-rootfs.version=3`。
- 目标目录：`F:\DSHA\dsha-1.2+`。该目录当前没有 `origin` remote。
- 目录选择说明：按要求扫描时还发现预先存在的
  `F:\DSHA\.dsha-1.2-v1.1.9.1-stage\settings.gradle`。它是升级前留下的临时脏副本，未作为构建目标、未被本轮编辑；本轮交付仍以用户明确指定的
  `F:\DSHA\dsha-1.2+` 为唯一工作树。
- 本地基线：`432bf87efe8fcd96370210eb52393fda17cc390a`（tag `v1.1.9.1`）；
  工作树位于本地分支 `dsha-1.2-local`，保留所有本地升级改动。
- 报告生成时间：2026-08-31（Asia/Tokyo）。

## 产物与签名状态

- offline rootfs：复用现有 `offline-rootfs.tar.gz`/`app/src/main/assets/offline-rootfs.bin`，大小 `312,535,854` 字节，
  SHA-256 `9835dea41a8a97373815cfb3b2d1ce86edbe01a7725ac220571b19ee8f21b095`，未重建 runtime 内容。
- 完整 APK：`release/dsha-1.2.0-alpha.1-vc102.apk`，大小 `406,488,276` 字节，
  SHA-256 `176ac2f0dcdf6abddd402b5e6f6876b132d8b85672f073bda6d03b07c0780f1e`。
- APK package 为 `com.dsh.client`，`versionName=1.2.0-alpha.1`、`versionCode=102`；`aapt` 确认仅
  `arm64-v8a` native code，APK 内含 `assets/offline-rootfs.bin`，且 zip entry 为 STORED（未二次压缩）。
- 最终交付 APK 经 `apksigner verify --verbose --print-certs` 通过 v2/v3；它使用已找到的历史
  publish/debug keystore 重签，证书 SHA-256 为 `e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`，与历史目标一致。
  未重签的 Gradle 临时 `app-debug.apk` 不作为交付包；覆盖安装条件仍须在授权测试机实测。
- `tools/pack-local.sh` 在 Windows Git Bash 的无 daemon lint worker 阶段异常退出；上面的 Android Build Tools
  `34.0.0` 手工 `apksigner` 核验才是本次记录的权威结果，且确认最新 APK 使用历史证书。
- 注意：当前 `offline-rootfs.tar.gz` 为既有本地包，未因本阶段 Java/UI 改动重建；Windows 侧只完成 Android 应用编译/打包，
  未完成一次在原生 Linux arm64 上从固定 commit 全程离线复现 upstream `build:official`、release pack 与 consumer install，
  因此不能把本产物描述为“native upstream full-build closure 已独立复现”。

## 已实施

1. 新增独立 `runtime_id`，保留历史 `use_rc6`、`workdir`、`api_key` 等偏好键；Alpha 只接受固定 runtime manifest 和生产 dsh 包。
2. `restore-merge.py` 使用独立 stage，候选 `.dsh` 先校验，再原子换位；rename、校验或中断失败时保留旧数据及失败 stage。
3. `ProotBootstrap.extractOfflineBundle()` 使用 stage、校验、同父目录原子切换、成功 marker 和失败回滚；失败不删除旧 rootfs、旧 marker 或旧用户数据。保留用户数据时，目录读取失败、未知条目或不能原样重建的软链都会 fail-closed，阻止切换而不是静默丢项或跟随复制。
4. Alpha 启动固定为 `dsh web --no-open --host 127.0.0.1 --port 3080`。只接受新版明确打印的严格 BrowserAuth URL，认证交换成功后才判定 Web 就绪；启动信息仅内存保存并按 generation 清理。
5. WebView/GeckoView 直接加载本轮 `authUrl`；移除 Alpha 路径中的 `dsha_t`、`X-Dsha-Token` 注入、裸 `uiUrl()` 探测及旧 WebView/auth/lan patch、自愈和会话格式改写。
6. LAN 默认关闭，由 DSHA 代理监听 `0.0.0.0:3081`；LAN token、3090 bridge token、dsh BrowserAuth cookie 独立。代理做 303 清 token、HttpOnly/SameSite=Strict LAN Cookie、loopback Host/Origin/Sec-Fetch-Site 重写、后端 Cookie/CORS 清理和 WebSocket 透传，并在认证失效时 fail-closed。
   BrowserAuth 交换兼容多个 `Set-Cookie` 响应头及合法的合并头，只提取本轮 `dsh-auth-*` Cookie，其余后端 Cookie 不会进入 LAN 代理。
7. Alpha 不自动注入旧插件、bundle、cordis patch、插件市场或会话迁移；旧 web profile 和 patch 在覆盖升级时隔离保存，session/settings/credentials 透明保留。
8. 日志、异常和 Web 输出增加敏感信息脱敏；活动日志、Android `/proc` 进程诊断和停止/复活诊断也在写盘或写 Logcat 前统一脱敏，不会把启动命令里的 API key、BrowserAuth token 或 Cookie 落盘。
9. `SensitiveData` 统一覆盖 query、header、Cookie、Bearer、异常文本及 JSON 引号字段（混合大小写），`LaunchFragment`、`DownloadSink`、PTY/终端、活动日志和 Web 日志均在展示或落盘边界脱敏；真实 HTTP 请求仍使用原始认证值。
10. Alpha 固定读取 APK 内置脚本资产，禁用 runtime-overlay、远端脚本检查和手动脚本更新入口；旧 runtime 的增量更新路径保留。
11. `scripts/ci-make-offline-bundle.sh` 每次都从全新的同父目录 rootfs stage 构建，不复用旧 `ci-rootfs`；只有产物通过结构和固定 runtime metadata 校验后才清理该 stage，失败现场保留供排查。
12. 便携式 Windows x64 工具链已落地到 `F:\DSHA\_toolchains`（Temurin 17.0.20.1、Gradle 8.5、SDK 34/build-tools 34.0.0/NDK 26.3.11579264、platform-tools），未改系统 PATH、注册表或全局 `JAVA_HOME`；Windows 侧 Java/资源编译、lint、完整 APK 打包均成功（Gradle unit test 为 `NO-SOURCE`）。本阶段额外使用历史 APK debug keystore 重签并通过证书核对；固定 dsh 的 production closure 仍必须在原生 Linux arm64 构建，未把 Windows x64 结果冒充 upstream arm64 构建。
13. 本阶段修复 HMR 单次自愈、PTY 弱生命周期 listener/下一帧刷新、Alpha 插件市场与 HMR 黑名单/安装回滚、四种透明备份范围（含 settings）和脱敏启动分段计时。

## 已运行测试

以下测试均在目标目录完成并返回成功：

| 命令 | 结果 |
| --- | --- |
| `tools/pure-logic-test.sh` | 357 项通过（含四种备份范围、HMR 判据及敏感字段） |
| `tools/phase2-pure-test.sh` | 38 项通过（HMR 一次重试、PTY attach/detach/帧刷新、插件回滚/黑名单、四种备份范围、未知归档拒绝与产物清理门禁） |
| HMR 自愈 `python -S` 夹具（无 PyYAML） | 通过：只对已知 `cordis-plugin-hmr` 错误追加禁用 entry，profile patch 写入后可读 |
| `tools/auth-url-test.sh` | 34 项通过 |
| `tools/archive-e2e-test.sh` | Java 12 项、shell 3 项通过；Windows Git Bash 下 POSIX 软链元数据断言按脚本跳过 |
| `python tools/restore-safety-test.py` | 通过（校验失败、rename 失败、中断与 Alpha packed session 场景） |
| `python tools/proot-extract-safety-test.py` | 通过（校验、rootfs/marker rename、marker 发布和切换失败） |
| `python tools/check-asset-scripts.py` | 通过：10 个 Python、15 个 shell、6 个 JS |
| `python -m py_compile app/src/main/assets/restore-merge.py app/src/main/assets/selftest.py` | 通过 |
| `tools/extract-roundtrip-test.sh` | 9 项通过；宿主不允许非特权软链，软链保护往返已明确跳过，须在 Linux/Android 复验 |
| `tools/restore-scope-test.sh` | 全部通过，含 P0 恢复失败注入 |
| `tools/stop-proc-test.sh` | 18 项通过；`/proc/net/tcp` 反查在当前 Windows 宿主不可用，按脚本标记附加层提示 |
| `tools/pack-local.sh --from 3 --to 5 --force` | 清单/编译阶段完成；assemble 阶段的无 daemon lint worker 在 Windows 宿主异常退出。随后直接 Gradle assembleDebug 成功生成同一完整 APK |
| `F:\DSHA\_toolchains\gradle-8.5\bin\gradle.bat --no-daemon -Pslim :app:compileDebugJavaWithJavac` | `BUILD SUCCESSFUL`，Java 17、SDK 34 |
| `F:\DSHA\_toolchains\gradle-8.5\bin\gradle.bat --no-daemon -Pslim lintDebug` | `BUILD SUCCESSFUL`，0 errors、332 warnings |
| `F:\DSHA\_toolchains\gradle-8.5\bin\gradle.bat --no-daemon -Pslim testDebugUnitTest` | `BUILD SUCCESSFUL`，`NO-SOURCE`（仓库没有 JVM 单元测试源） |
| `F:\DSHA\_toolchains\gradle-8.5\bin\gradle.bat :app:assembleDebug --offline` | `BUILD SUCCESSFUL`，使用现有 rootfs 资产生成完整 APK |
| Android ARM64/Termux 预检 | ADB 设备报告 `aarch64`/Android 13，但当前 shell 无 `su`；未执行 root/chroot 构建或写入设备数据 |
| ARM64 offline rootfs | 复用现有包，SHA-256 `9835dea41a8a97373815cfb3b2d1ce86edbe01a7725ac220571b19ee8f21b095`；本机未完成原生 arm64 closure/chroot 验证 |
| `apksigner verify --verbose --print-certs` | 通过 v2/v3；最新 APK 证书 `e7e3…a53f5` 与历史目标一致 |
| `aapt dump badging` 与 `aapt list` | `com.dsh.client` / `1.2.0-alpha.1` / `102`、只含 `arm64-v8a` native code，内含 STORED offline rootfs asset |
| Git Bash 复跑纯逻辑/资源/安全测试 | 357 项纯逻辑、15 项 phase2、34 项 auth URL、归档/提取/恢复安全、停止进程与脚本检查均通过 |
| Android 设备只读预检 | 单台已授权设备：Android 13、`arm64-v8a`、`aarch64`；`su -c id` 不可用（root unavailable），`unshare --net --fork true` 返回 `Operation not permitted`，未安装 `com.dsh.client`；未执行安装或写入应用数据 |
| 历史 APK/密钥核对 | 参考 APK 与本地历史 debug keystore 均为目标 `e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`；最新 APK 已用该 keystore 重签并核验 |
| `python tools/gen-runtime-manifest.py --check` | `MANIFEST_OK`（35 个文件） |
| runtime manifest 签名 | 使用本地 `DSHA-release.keystore` 生成，`SIGN_OK`，并由 `assets/runtime-update-pubkey.pem` 自验通过 |
| 敏感日志静态审计 | 通过：query/header/Cookie/Bearer/JSON/异常/URL 展示边界统一走 `SensitiveData`；真实 HTTP 请求仍保留原始认证值；未发现 Alpha 路径把完整 auth URL、dsh-auth Cookie、LAN/3090 token 写入日志、Toast、Preferences 或响应头 |
| `out/` 日志敏感值扫描 | `NO_UNREDACTED_VALUES`；测试日志未发现未脱敏 token、Cookie、Authorization 或 API key |
| `bash -n scripts/ci-make-offline-bundle.sh`（Git Bash） | 通过 |
| `git diff --check` | 通过 |

## 第二阶段复核证据（2026-08-30）

- 项目私有工具链复核：Temurin `17.0.20.1`、Gradle `8.5`（Gradle JVM 为 Temurin 17）、Android SDK 34、Build Tools `34.0.0`、NDK `26.3.11579264`、adb/apksigner/sdkmanager 均可执行；未修改系统 PATH、注册表或全局 `JAVA_HOME`。
- Windows x64 应用侧编译、资源检查和 lint 已成功；这不等同于 dsh upstream production closure 构建。固定 dsh 的 `build:official`、release pack、clean consumer packed-install 与 offline rootfs 必须在原生 Linux arm64 执行，当前因主机能力不足未声称成功。
- 设备预检：唯一设备 `fmuoeujvonizjj4d` 为 Android 13 / `aarch64` / `arm64-v8a`，当前 ADB shell 无 `su`，未安装或写入应用数据；不能据此完成原生 Linux arm64 rootfs 构建门禁。
- 清单检查：`python tools/gen-runtime-manifest.py --check` 返回 `MANIFEST_OK`（35 个文件）；使用本地 `DSHA-release.keystore` 重签，`SIGN_OK` 并由 APK 内公钥自验通过。
- 敏感日志审计：覆盖 query、header、Cookie、Bearer、JSON、异常文本、Web 输出、Toast、活动日志、下载异常和网络响应头；测试与 `out/` 扫描均返回 `NO_UNREDACTED_VALUES`。真实请求仍使用原始认证值，展示/日志边界才做脱敏。
- 参考 APK 与本地历史 debug keystore 的证书指纹只读核对均为 `e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`；最新 APK 已用该 keystore 重签并通过核验。

## 尚未完成及硬阻塞

- Windows x64 工具链已完成 APK 层编译/打包；完整 APK 存在且签名匹配历史证书。`app/src/main/assets/offline-rootfs.bin` 复用本地 `offline-rootfs.tar.gz`，二者 hash 一致。
- 未检测到可控的原生 Linux arm64 构建主机；固定 upstream 的 official build、release pack、clean consumer packed-install、rootfs/chroot WebAuth 仍未完成，不能声称 offline rootfs 通过 arm64 门禁。
- 未完成全新安装、带旧 `.dsh` 覆盖升级、会话重启持久化、旧 session 校验和真机 LAN e2e；本轮 `TEST_DEVICE_SERIAL` 与 `DSHA_DEVICE_TEST_OK` 均未设置，未安装 APK。3080/3081 的真实局域网可达性只能由后续具备 root/chroot 能力的 Android/Linux arm64 环境验证。
- APK 已用历史证书通过 `apksigner` v2/v3 核验，但设备安装、Web/PTY/备份恢复/LAN 功能仍未验证；不得把本地签名核对替代真机覆盖测试。

## 边界确认

- 旧参考树 `F:\DSHA\dsha\deepseekharness\deepseekharness` 本轮仅执行只读 `git status --short --branch`；检查时已有未提交改动，未执行编辑、格式化、reset/clean/checkout 或会写入它的工具。
- 本轮没有执行 GitHub 写操作：没有 `git push`、`gh` 写命令、PR、Issue、Release、Tag、Actions 或远端分支操作。
- 目标树 `git remote -v` 为空；所有构建/测试日志均写入本地 `out/`，不上传远端。
- 未完成项均保留在报告中，不能替代 APK、arm64 rootfs 或真机验证结论。

## 收尾复核（2026-08-30）

- 使用项目私有 Temurin 17 + Gradle 8.5 串行复跑 `:app:compileDebugJavaWithJavac`、`lintDebug`、`testDebugUnitTest`：Java 编译通过，在线依赖解析后的 lint 通过（0 errors，仓库既有 warnings），单测仍为 `NO-SOURCE`。并行运行曾使资源清理任务互相竞争，已改为串行确认；`--offline lintDebug` 的失败原因是本机 Gradle cache 缺少 AndroidTest 的 `core-ktx/lifecycle/coroutines`，不是源码错误。
- 使用 Git Bash 绝对路径复跑 phase2 纯逻辑 15 项、纯逻辑 357 项、auth URL 34 项、归档/恢复/rootfs/停止进程测试和资产脚本检查，全部通过；当前 PowerShell 的 `bash` 入口仍指向不可用 WSL，因此没有把该执行器错误计入代码失败。
- 重新核对过的上一候选 APK（`006008e7…`）已被本轮 latest/UI 修复后的 release 包替代；其 offline rootfs 与 APK 资产 SHA-256 均为 `9835dea41a8a97373815cfb3b2d1ce86edbe01a7725ac220571b19ee8f21b095`。
- 历史 publish/debug keystore 已在本地找到，并已对候选 APK 完成 `apksigner verify --print-certs` 核验；runtime manifest 仍为 `MANIFEST_OK`，签名自验证为 `SIGN_OK`。密钥路径和口令不写入报告。
- `git diff --check` 通过；`app/src/main/assets/__pycache__/` 仅为被 `.gitignore` 忽略的本地 Python 测试缓存，不进入 runtime manifest 或 APK。
- 收尾只读核验：旧参考 APK `deepseekharness-arm64-v1.1.9-rc6-616f5c.apk` 为 `com.dsh.client` / `1.1.9-rc6` / `versionCode=97` / `arm64-v8a`，v2/v3 与历史证书指纹均匹配；当前设备虽在线，但 `TEST_DEVICE_SERIAL` 和 `DSHA_DEVICE_TEST_OK` 未设置，因此没有执行安装、覆盖升级或任何设备数据写操作。

## 本轮修复与产物收敛（2026-08-31）

- 工作区新建入口恢复为互斥的四项范围选择：“备份全部数据 / 仅备份对话记录 / 仅备份设置 / 仅备份插件”，每项显示一句说明并在确认框再次展示“即将备份”范围；恢复结果显示备份类型、创建时间和包含内容。旧版无清单备份仍按全量兼容恢复。
- 恢复会在解压前完整读取 gzip/tar；损坏包直接拒绝，清单 scope 未知或不含 `.dsh`、会话、设置特征的归档也会明确报错并在创建恢复 stage 前退出，现有数据不被覆盖。
- 新建备份统一为 `DSHA-backup-latest.tar.gz`。范围写入包内 `.dsha-backup-manifest.json`，恢复以清单为准而不是固定文件名猜测；Android 10+ 先写临时 MediaStore 条目，写完并发布后才删除旧 latest；Android 9- 先写同目录临时文件再 `ATOMIC_MOVE`，失败注入验证旧文件字节保持不变。
- 部分备份默认不复制 API Key，也不删除当前 `.dsh` 中已有的 key 文件；只有全量/设置范围且用户打开现有开关时才把加密快照加入包。
- GitHub 输入框移到插件页独立整行，最小宽度 220dp、高度 42dp、单行横向滚动，长链接不会被 ellipsize 截断。
- `tools/pack-local.sh` 成功后只发布 `release/dsha-1.2.0-alpha.1-vc102.apk` 与同名 `.sha256`，并清理目标树 root/out/app/build/outputs 中旧 APK、candidate、idsig 以及 `out/pack-local-*`/`out/full-build` 临时打包目录；offline rootfs、runtime manifest/signature、源码、测试和文档保留。
- 本轮测试：`tools/phase2-pure-test.sh`、`tools/backup-latest-test.sh`、`tools/pure-logic-test.sh`（359 项）、`tools/restore-scope-test.sh`（sessions/plugins/settings/legacy full 及 P0 注入）、恢复/rootfs 失败注入、Java 编译、`lintDebug`、`testDebugUnitTest`（NO-SOURCE）、完整 assemble、`aapt`、Windows `apksigner.bat`、rootfs STORED 检查、`git diff --check` 均通过。最终 APK SHA-256 为 `176ac2f0dcdf6abddd402b5e6f6876b132d8b85672f073bda6d03b07c0780f1e`，证书指纹匹配历史值 `e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`。
- QQ 群展示与 `mqqapi` 跳转统一更新为 `975836806`；GitHub/QQ 入口仍只在本地 App 中生效。
- `tools/pack-local.sh` 现可识别 Windows `apksigner.bat`，并在清单阶段探测 `python3` 占位别名后回退到可运行的 `python`，避免错误跳过签名门禁。
- 本轮未安装 APK。由于未设置 `TEST_DEVICE_SERIAL` / `DSHA_DEVICE_TEST_OK`，真机覆盖升级、Web/PTY、插件市场、备份恢复、LAN 和三次启动耗时仍未验证；没有可控原生 Linux arm64 主机，upstream production closure/chroot WebAuth 仍是硬阻塞。未执行任何卸载、清数据或用户备份删除。

## 2026-08-31 最终收尾

- 重新使用历史 publish/debug keystore 完成 `tools/pack-local.sh --only 6` 与 `--only 7`；发布目录仅保留
  `release/dsha-1.2.0-alpha.1-vc102.apk` 及同名 `.sha256`，APK/校验文件、candidate 和 idsig 已从目标树的旧生成目录清理，
  `offline-rootfs.tar.gz`、runtime manifest/signature、源码、测试与文档保留。
- 最终 APK SHA-256 为 `176ac2f0dcdf6abddd402b5e6f6876b132d8b85672f073bda6d03b07c0780f1e`；
  `aapt dump badging` 确认为 `com.dsh.client`、`1.2.0-alpha.1`、`versionCode=102`、仅 `arm64-v8a`；
  `assets/offline-rootfs.bin` 存在且为 ZIP STORED（312,535,854 字节，未二次压缩）。
- `apksigner verify --verbose --print-certs` 通过 v2/v3，证书 SHA-256 为
  `e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`；`gen-runtime-manifest.py --check` 返回 `MANIFEST_OK`，
  runtime manifest 签名自验证返回 `SIGN_OK`。
- 最终复跑 `phase2-pure-test`、`backup-latest-test`、`pure-logic-test`（359 项）、`auth-url-test`、归档/提取/恢复/rootfs
  失败注入、资产脚本检查、Python 编译和全部 shell 语法检查，均通过；敏感日志扫描保持 `NO_UNREDACTED_VALUES`。
- 仍未完成原生 Linux arm64 upstream production closure/chroot WebAuth 与真机安装、覆盖升级、Web/PTY、备份恢复、LAN 和三次启动耗时验收；
  本轮没有安装 APK、卸载应用、清理数据或删除手机备份。

## Alpha2 继续执行（2026-08-31）

- 本地分支仍为 `dsha-1.2-local`，版本固定为 `1.2.0-alpha.2` / `versionCode=103`，
  upstream 固定为 `dsh-v0.1.2-alpha.2` @ `0a53fb55bea101816fa226bb964ae2bed71c343b`，
  `OFFLINE_VERSION=4`。目标树 remote 为空，旧参考树和 GitHub 均未执行写操作。
- 使用项目私有 Temurin 17.0.20.1、Gradle 8.5 串行复跑
  `:app:compileDebugJavaWithJavac -Pslim`、`lintDebug -Pslim`、
  `testDebugUnitTest -Pslim`，全部 `BUILD SUCCESSFUL`；单元测试仍为 `NO-SOURCE`。
  `tools/pure-logic-test.sh`（359 项）、`tools/phase2-pure-test.sh`（38 项）、
  `tools/auth-url-test.sh`（34 项）、`tools/backup-latest-test.sh`、资产脚本检查、
  Python 编译及全部 shell 语法检查均通过。
- `:app:assembleDebug --offline` 可在 Windows 完成，但当前 `offline-rootfs.tar.gz` 内的
  runtime metadata 和 dsh package 仍明确为 Alpha1（commit `cd5ef814...`）。该包因此只是
  Alpha2 外壳与 Alpha1 runtime 的混合中间产物，已从 `app/build/outputs` 删除，未复制到
  `release/`，也未作为可安装交付物或签名结论。
- 当前 `offline-rootfs.tar.gz` SHA-256 仍为
  `9835dea41a8a97373815cfb3b2d1ce86edbe01a7725ac220571b19ee8f21b095`；在 native arm64
  closure、rootfs/chroot WebAuth 成功之前，不替换该包、不生成 Alpha2 APK，避免混合版本。
- Termux/USB 调试设备的 SSH 服务端口（8022）本轮持续不可达；没有可控 root/chroot 能力，
  不能用 adb、QEMU、WSL、Docker 或手机端临时 npm install 代替 native Linux arm64 门禁。
  因此 Alpha2 production closure、offline rootfs、BrowserAuth 真实启动、真机覆盖升级、
  LAN/PTY/备份 e2e 和三次启动耗时仍标记为未验证。未安装 APK、未卸载/清数据、未删除任何
  `.dsh` 或手机备份。
- 本轮最后执行 `git diff --check`，通过；release 中仅保留既有 Alpha1 回滚 APK 及其校验文件。

## Alpha2 rootfs 与 APK 验收（2026-08-31）

- 在已授权的 Termux root 环境中以 `uname -m=aarch64`、`su uid=0`、`chroot`、`mount` 和
  `unshare --net` 完成 native arm64 组装；没有使用 adb、QEMU、WSL、Docker 或手机端在线安装。
  upstream `build:official` 和 release pack 产物经过 clean consumer 依赖闭包检查，并补入
  native `sharp`（`@img/sharp-linux-arm64`/libvips）与 `koffi`（`@koromix/koffi-linux-arm64`）
  运行依赖。最终 offline rootfs SHA-256：
  `85a9c4ac4726b81e99a7290f9d040afd842831b254d3c3e43f756973f5fb53a6`。
- rootfs 内真实执行 `dsh web --no-open --host 127.0.0.1 --port 3080`：Alpha2 版本打印正确，
  裸 `/` 返回 `401`；官方 auth URL 返回 `303 Location: /` 并设置 `dsh-auth-*` Cookie，
  带该 Cookie 二次访问返回 `200`。首次 HMR 错误已复现并确认移动端应切换 profile
  `patchReload` 到 `startup`；Android 侧新增原子 profile 回退，保留单次重试且不改会话/凭据。
- 生产运行时 closure（含 arm64 可选原生依赖）SHA-256：
  `d24b333429252c264db853148b64b616d72d54c2a2be79ccdc037bcd5eba863f`；离线 rootfs
  metadata 固定为 Alpha2 commit，未混入 upstream 源码树、pnpm store 或开发 checkout。
- 使用历史证书重新构建完整 APK：
  `release/dsha-1.2.0-alpha.2-vc103.apk`，SHA-256
  `023e004ee12c5feceb693ec5c42883e8e1d7384bd68313f1696b91858a2badec`。
  `aapt` 核验 `com.dsh.client`、`versionName=1.2.0-alpha.2`、`versionCode=103`、仅
  `arm64-v8a`，内含 `assets/offline-rootfs.bin` 且为 ZIP `Stored`；`apksigner` v2/v3 通过，
  证书 SHA-256 为历史值 `e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`。
- 本轮串行复跑 Java 编译、lint（0 errors）、`testDebugUnitTest`（`NO-SOURCE`）、359 项纯逻辑、
  38 项 phase2、34 项 auth URL、备份/恢复/rootfs 失败注入、资产脚本和 shell/Python 语法检查；
  `runtime-manifest.py --check`=`MANIFEST_OK`、manifest 自验=`Verified OK`、`git diff --check` 通过，
  `out/` 敏感值扫描保持 `NO_UNREDACTED_VALUES`。
- 真机安装/覆盖升级、PTY/插件市场/备份恢复/LAN e2e 和三次启动耗时尚未执行；未设置授权环境变量，
  未安装或卸载 APK、未清理应用数据、未删除 `.dsh` 或手机备份。Alpha1 APK 保留在 release 作为回滚包。

## Termux 真机安装验收（2026-08-31）

- 经用户明确授权，未使用 adb；通过 Termux SSH（`192.168.2.11:8022`）以 root `su` 执行保留数据安装。
  APK 传输后 SHA-256 与本地产物一致：`023e004ee12c5feceb693ec5c42883e8e1d7384bd68313f1696b91858a2badec`。
- `pm install -r` 返回 `Success`。安装后包信息确认 `com.dsh.client`、`versionName=1.2.0-alpha.2`、
  `versionCode=103`、`primaryCpuAbi=arm64-v8a`、APK signing version 3；未执行卸载、降级、`pm clear` 或数据删除。
- `MainActivity` 可启动，进程存活；停止后重启仍可创建进程，logcat 采样未发现 `FATAL EXCEPTION` 或 `AndroidRuntime`。
- 设备当时处于锁屏状态，UI 自动化树仅返回系统 Keyguard；未绕过锁屏，因此 WebView/BrowserAuth、PTY、插件市场、
  备份恢复、LAN 和启动耗时的交互验收仍未完成。应用数据目录未做删除或清理。

## Termux 解锁后交互验收（2026-08-31）

- 用户解锁设备后，通过 Termux SSH 完成界面只读验收。启动页显示“DSH 运行中 / Web UI 已启动”，WebView
  成功加载 DeepSeek Harness，并出现“添加一个 API Key”官方页面提示；启动日志中的 auth URL 已脱敏。
- 插件页可进入“插件市场”，展示 2742 个插件；GitHub 插件链接输入框为独立整行，设备宽度约 970px，未发生标题挤压。
- “数据与备份”页显示“选择备份范围 / 恢复 / 查看保存位置”；范围弹窗实际显示四项互斥选项：全部数据、对话记录、设置、插件，
  每项包含说明。未执行写入备份或恢复操作，避免改变测试设备数据。
- 终端页可进入，显示 Ubuntu · PTY、终端视图以及 ESC/TAB/CTRL/ALT 和方向键控件；未执行破坏性命令。
- `/proc/net/tcp` 只见 `127.0.0.1:3080` 监听及本机连接，未见 3081 监听，LAN 代理保持默认关闭；未改变 LAN 偏好。
- 该设备为全新安装（安装前无 `com.dsh.client`），因此本次不是覆盖升级测试；旧 `.dsh`、会话持久化、备份恢复实际写入、LAN 授权、
  PTY 旋转重绑和三次启动耗时仍需单独测试。未执行卸载、清数据或删除用户备份。

## Mobile-nav 永久移除与 vc104 修复包（2026-08-31）

- 已从 APK assets、离线 provisioning、profile merge、stale/selftest 链路和 Java 内置插件注册链路移除 `@dsh-external/dsh-mobile-nav`；`dsh-web-mobile` 保留。升级启动仅清理该插件自身的实体、链接、marker 和 profile 声明，不触碰 `.dsh`、会话、凭据或用户备份。
- 由现有 alpha2 rootfs 生成无 mobile-nav 包：`offline-rootfs.tar.gz` / `app/src/main/assets/offline-rootfs.bin`，SHA-256 `d72d00e640d5b29b63cf5d09dbc4338bdd42ed8f23fbd0926cacb7f691227014`；tar 检查确认无 mobile-nav 路径且含 `dsh-web-mobile`。
- 重新构建并替换错误的 vc104 产物，最终文件为 `release/dsha-1.2.0-alpha.2-vc104.apk`，SHA-256 `f51f97969723521878e4e4898c22adf02fa9a4f761c8b5fad8e28821d7825198`。`aapt` 确认包名 `com.dsh.client`、版本 `1.2.0-alpha.2`/`104`、仅 `arm64-v8a`，rootfs 为 ZIP Stored；`apksigner` v2/v3 通过，证书指纹为历史值 `e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`。
- 本地验证通过：Java 编译、lint（0 errors）、`testDebugUnitTest`（NO-SOURCE）、纯逻辑 359 项、phase2、Python 编译、资产脚本检查、runtime manifest `MANIFEST_OK`、manifest `SIGN_OK`、`git diff --check`。
- Termux 真机诊断确认设备为 Android 13/aarch64，旧安装为 alpha2 `versionCode=103`，`.dsh` 与凭据仍存在；通过 SSH 已传输 vc104 APK，但大文件传输后设备 SSH/banner 与 ping 暂时不可达，尚未执行 `su -c pm install -r`，因此 Web UI/HMR/PTY/插件/LAN/备份交互验收仍未完成。未执行卸载、`pm clear`、降级或任何数据删除。

## vc104 Termux 覆盖与 Web 验收（2026-08-31）

- 通过 Termux SSH（未使用 adb）分片传输并校验 APK，设备端 SHA-256 与本地产物一致；`su -c pm install -r` 返回 `Success`。安装前后 `.dsh` profile、settings、credentials 和 storages 文件 SHA-256 保持不变。
- 设备包信息确认 `com.dsh.client`、`1.2.0-alpha.2`、versionCode `104`、`primaryCpuAbi=arm64-v8a`。停止应用后再次启动，`libproot.so` 与 Node 进程重新创建，Web 端口恢复监听。
- Web 验收：`127.0.0.1:3080` 正常监听，裸 URL 返回 `401 Unauthorized`；dsh 日志仅输出脱敏 BrowserAuth URL；未发现 `FATAL EXCEPTION`、`AndroidRuntime`、`--expose-internals` 或 `cordis-plugin-hmr` 错误。设备 profile 不含 `mobile-nav`，rootfs 中无该插件路径。
- 已删除设备端传输临时分片和临时 APK；未卸载、未 `pm clear`、未删除 `.dsh` 或用户备份。由于设备屏幕随后回到锁屏/黑屏，无法通过 UI 自动化读取 WebView DOM，因此 Cookie 交换后的页面可见性仍保留为未完成项；loopback 监听、401 安全边界和进程重启已实测通过。
## 2026-09-01 真机存储可见性复测

- 通过 Termux SSH（未使用 adb）检查设备 `com.dsh.client` 与 `bin.mt.plus`。
- 两个应用的 `MANAGE_EXTERNAL_STORAGE` app-op 均为 allow，但 MT 仍无法添加 DSHA。
- 根因：DSHA 原 APK 未注册 Android `DocumentsProvider`，MT/系统 SAF 没有可枚举的 DSHA 根。
- 新增 `DshaDocumentsProvider`，仅在用户通过文件管理器明确授权后暴露 App 私有 rootfs 文件树，路径校验限制在 DSHA rootfs 内。
- 已通过 `:app:compileDebugJavaWithJavac`；完整 release APK 和设备覆盖验证待本轮构建完成后记录。
- release 构建成功，APK 已通过 v2/v3 签名校验；历史证书指纹保持不变，SHA-256：`5c1b753741d5db8e4d1d2c0a89795a6ced511d43689fe9281b3f114b07152d95`。
- 通过 Termux SSH 执行 `su -c pm install -r` 覆盖安装成功，未卸载、未清理数据。
- 设备包管理器已确认注册 `com.deepseekharness.app.DshaDocumentsProvider` 及 `android.content.action.DOCUMENTS_PROVIDER`，供 MT 的 SAF 本地存储入口枚举；未改动用户数据。
### 2026-09-01 恢复与真机复测补充

- 追加离线 Python3：将 Termux arm64 Python 3.14.6（解释器、libpython 与标准库）打入 `app/src/main/assets/runtime-python/python-runtime.tgz`；候选 rootfs 安装阶段自动解包并校验，现有设备通过 Termux 手工解包到 rootfs 后确认 `/bin/python3 --version` 正常。
- 系统 `content query` 已能枚举 `content://com.dsh.client.documents/root`，返回根标题 DSHA；MT 自身“添加本地存储”仍不显示该 provider，属于 MT 枚举过滤/兼容性问题，非 provider 未注册。

- 真机通过 Termux SSH（`su -c pm install -r`）覆盖安装当前 vc104，未卸载、未清理数据。
- 修复恢复入口：执行 `restore-merge.py` 前探测 rootfs 内 `python3`/`python`；缺失时明确提示运行时缺少 Python3，并保留独立 stage 与现有 `.dsh`，不再笼统显示“未确认落位”。有解释器时仍只接受 `RESTORE_OK`/`RESTORE_PARTIAL` 标记。
- DocumentsProvider 已注册并增加 `FLAG_LOCAL_ONLY`，根标题为“DSHA”，供 MT/SAF 枚举；设备包管理器确认 authority `com.dsh.client.documents` 已注册。MT 界面实际枚举仍需人工确认。
- 设备 rootfs 的四个 Alpha 插件实体及 `profile/node_modules` 链接已通过只读检查；未发现 `mobile-nav` 实体或 profile 注册。
- 当前 APK：`release/dsha-1.2.0-alpha.2-vc104.apk`，SHA-256 `4a9bf7dca5a0fdaae77c77f039531d81cd1a8f3990b26e24acacfda631935d8b`。签名证书 SHA-256 仍为 `e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`，v2/v3 验证通过，包名/版本/ABI 检查通过。
- 本轮 `restore-merge.py` Python 编译、恢复安全测试和 `git diff --check` 通过；完整 Web UI 真机启动与 MT UI 枚举尚未在本轮通过界面操作确认。

- MT 兼容修复：旧版依赖的 `com.github.L-JINBIN:MTDataFilesProvider:v1.0.0` 原先误放在 `debugImplementation`，导致 release APK 不含 `bin.mt.file.content.MTDataFilesProvider`/`MTDataFilesWakeUpActivity`。现已改为 release `implementation`；新 APK Manifest 与设备 `dumpsys package` 均确认该 provider、authority `com.dsh.client.MTDataFilesProvider` 和唤醒 Activity 存在，`content query` 返回 DSHA 根。请在 MT 中重新打开“添加本地存储”页面使其刷新枚举。
- 恢复补丁：覆盖升级后旧 rootfs 不会自动重新解压 APK 资产，故恢复入口现在先调用 `ensureBundledPython()` 幂等补齐 Python3，再优先使用绝对路径 `/bin/python3` 执行合并脚本；当前 APK SHA-256 更新为 `49c43feff44b11c7b4a60c3995ae81c74950a86689d7ad305d95c3eadfbb5e88`。
- 本轮 Windows 构建完成且签名保持历史证书；由于设备 Termux `sshd` 当前拒绝连接，尚未能把该最后构建覆盖安装到真机，需恢复 Termux SSH 后再执行一次 `su -c pm install -r` 验收恢复按钮。
- 2026-09-01 恢复最终根因：Termux Python 的 ELF interpreter 是 `/system/bin/linker64`，RUNPATH 固定为 `/data/data/com.termux/files/usr/lib`。旧打包仅把运行时放进 rootfs `/lib`，宿主直接运行正常但 App 的 proot 内报 `cannot execute: required file not found`，因此没有任何 RESTORE marker。
- 修复后 Python 资产保留真实前缀布局，额外包含 `libandroid-support.so`；proot 对存在的 `/system`、`/apex`、`/linkerconfig` 做 bind。真机真实 proot 内已验证 `Python 3.14.6`，并用同一恢复 stage、隔离目标 root 执行 `restore-merge.py`，得到 `RESTORE_DSH_COMMITTED` 与 `RESTORE_OK`，未触碰在线 `.dsh`。
- 修复版已通过 Termux `su -c pm install -r` 覆盖安装；APK SHA-256 更新为 `97f3cf5cfe5b9d67c1d8c3ffe6f4b0cdaa8178277271b24f13e8707b327f1079`。

### 2026-09-01 Python3 路径兼容修复复测

- 真机检查发现覆盖升级后的旧 rootfs 将离线 Python 解包到了 `ubuntu/root/bin` 与
  `ubuntu/root/data`，而恢复入口按 proot 根目录执行 `/bin/python3`，因此界面仍提示“当前离线运行缺 Python3”。
- `ProotBootstrap.ensureBundledPython()` 现会识别该旧布局，在不删除或改写 `.dsh` 的前提下，
  将解释器、标准库和 Android linker 支持库幂等补齐到 rootfs 的 `/bin`、`/data` 前缀；
  仍不注入 `--expose-internals`，失败时保留原数据与 stage。
- 通过 Termux SSH（未使用 adb）执行 `su -c pm install -r` 覆盖安装 vc104，返回 `Success`；
  设备端随后验证 `/usr/bin/python3 --version` 为 `Python 3.14.6`。安装过程未卸载、未清数据，
  既有 `.dsh` 与备份未删除。
- 本地 APK 重新构建并核验：`release/dsha-1.2.0-alpha.2-vc104.apk`，SHA-256
  `e90016315ad88c0331fa41e0b457b12f1c0ce8c8d9a684745df954f7bb10bc02`；包名、版本、arm64、
  v2/v3 及历史证书指纹均通过。完整恢复按钮的第二次 UI 点击因设备自动锁屏未完成，需保持屏幕常亮后复测。
- 随后修正 `DshaDocumentsProvider` 的 API 26 lint 问题并重建最终包；最终 APK SHA-256 为
  `78afda436d7e7884501b1729a20323add9806f4f6dac1f5a7dcf0b67f50eca0e`，已再次通过 Termux
  SSH `su -c pm install -r` 覆盖安装并返回 `Success`。设备包管理器确认版本 104；rootfs
  `/usr/bin/python3` 与标准库仍可执行，既有 `.dsh` 保持存在。
